const styles = StyleSheet.create({
  view:{    
  },
  container: {
    flex: 1,
    justifyContent: "center",
    paddingHorizontal: 16,
  },
  input: {
    marginBottom: 20,
  },
  button: {
    marginBottom: 20,
  },
  mensaje: {
    fontSize: 20,
    textAlign: "center",
  },
});

export default styles;
